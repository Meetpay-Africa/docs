---
title: 'Ping'
openapi: 'GET /api/v1/ping'
---

Lightweight connectivity and credential check — use it to verify an API key before going live. Returns `merchant_id`, `merchant_name`, the key's `environment` (`production` or `test`), and `key_name`, with the message `pong`.
